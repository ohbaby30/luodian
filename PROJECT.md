# Project 落点

**当前版本：2.0.0**

**版本状态：** 2.0.0 的源码、数据库迁移、交付文档和本地验证已完成；真实服务器部署与真实 Token Plan 凭证调用仍按部署现场单独确认。

## 项目目标

交付一个单用户、局域网访问的个人想法收口网页：用户输入原始想法，系统识别目标、事实、假设、约束、漏洞、缺口、风险和验收标准，逐次提出一个会改变结果的关键问题，并在信息足够后生成可复制的高质量提示词。

## 当前范围

- 单用户、局域网访问；通过 Docker Compose 启动。
- 首次打开网页完成访问密码，以及至少一组 AI 兼容 API 地址、API Key 和模型设置；可同时保存普通 API 计量与 Token/Coding Plan 两组配置。
- 支持个人规则档案，并在每次收口时作为上下文使用。
- 支持通用对话、Codex/代码执行、深度研究、内容创作和决策分析等提示词目标。
- 保留想法、问答过程、分析快照和提示词版本；支持归档与 Markdown/JSON 导出。
- AI 只补充必要缺口；额外创意进入可选延伸，不自动写入最终提示词。
- 首版仅支持纯文本，不执行生成的提示词，不做多人协作、公开互联网访问、Ollama、本地模型、文件上传或外部服务集成。

## 技术与部署决策

- Next.js + TypeScript 单体应用。
- SQLite（使用 Node 内置 `node:sqlite`，避免原生 addon 在不同 Node 版本上的编译差异）；运行数据保存在 Compose 数据卷。
- Zod 校验 AI 结构化结果；AI 使用可配置的 OpenAI-compatible Chat Completions 接口，普通 API 计量与 Token/Coding Plan 配置相互独立。
- Tailwind CSS 和可访问的组件基础；界面以简体中文为主。
- Debian 上使用 Docker Compose；默认局域网端口为 `3088`，可通过 Compose 环境变量调整。
- API Key 加密保存；访问密码只保存不可逆哈希；个人想法保存在服务器数据卷中。
- 除使用 Node 内置 SQLite 以保持 Mac 与 Debian 的安装一致外，以上技术选型无特殊考量，默认选择。
- 2.0.0 没有更换主技术栈，也没有新增外部运行时服务：仍为 Next.js 15、React 19、TypeScript、Tailwind CSS、Zod、Node.js 22 和 Node 内置 SQLite；本版本主要扩展数据模型、Provider 适配层、工作流 API 和页面能力。

## 当前实现进度

- 2.0.0 交付内容：需求简报与技术问题分流、回答/委派/强制收口、普通 API 与 Token/Coding Plan 双配置及协议适配、已归档独立分页、首页归档过滤、JSON/Markdown 导出、窄屏 UI 适配和山景背景视觉改版。
- 2.0.0 本地质量门槛：21 个测试文件、64 个测试通过；TypeScript 检查和 Next.js 生产构建通过；交付副本已按文件路径复核，未包含数据库、环境文件、密钥或内部测试资料。

- v1 核心流程已经实现：首次设置、单用户登录、创建想法、必要缺口追问、分析快照、最终提示词生成、版本保存以及 Markdown/JSON 导出。
- 需求补全与专业问题分流已实现：新分析使用 v2 完整需求简报（目标、背景、交付物、范围内/外、优先级、偏好和验收标准），技术细节进入 Agent 自行研究/决策列表；用户意图、范围、优先级、授权等问题仍保留为一次一个的关键追问。
- 当前追问支持回答或手动委派给执行 Agent；正常收口只接受已回答/已委派的问题，二次确认后的强制收口会把未确认内容记录为开放决策，并在最终提示词中要求执行 Agent 使用可逆、保守的默认值。
- 追问记录新增 `resolution`（`pending`、`answered`、`delegated`）；应用启动和 Docker 初始化脚本会为旧数据库自动补列，并按已有回答回填，不删除现有数据。Markdown/JSON 导出会包含完整分析和每条追问的处理结果。
- 公共 GitHub 发布历史：仓库为 `https://github.com/ohbaby30/luodian`，已包含公开 README 和 MIT LICENSE；`v1.0.0` 是 1.0 稳定部署恢复标签，2.0.0 将以 `v2.0.0` 标签发布。
- 2026-09-10 用户确认已在服务器完成上传、部署和实际测试，部署成功；该状态来自用户的服务器实测反馈，本 Agent 未通过 SSH 独立复核服务器日志或运行状态。
- AI 请求适配已拆为独立兼容层：普通 OpenAI-compatible 服务默认使用 `Authorization: Bearer` 和 `max_tokens`；识别 Xiaomi MiMo Token Plan 的 `/v1` Base URL 时使用官方要求的 `api-key` 和 `max_completion_tokens`；MiMo/LongCat 等已知模型按协议关闭 thinking 或启用 JSON mode；其他 Token/Coding Plan 可在高级设置中覆盖鉴权头、输出参数、JSON mode 和 thinking。所有请求明确使用非流式模式，连接测试要求返回 `{"ok":true}`，不再把任意 `2xx` 响应当作成功。真实 Token Plan 仍需使用有效 `tp-` Key 单独复测。
- 两组 Provider 分别保存在 `settings.provider_profiles_json` 中，API Key 仍逐组加密保存；旧版单配置会按已有协议特征迁移为兼容槽位。两组都完整时，新想法首次分析必须选择接口，之后通过 `ideas.provider_kind` 绑定，追问和提示词生成不会中途切换。
- Provider 高级选项保存在每组配置的 `providerOptions` 中；应用启动和 Docker 初始化脚本会为旧数据库幂等补列，未配置过的旧记录保持自动识别。
- AI 测试连接和保存设置的成功/失败反馈已移入 AI 接口卡片内部；普通 API 与 Token/Coding Plan 各自显示独立的测试反馈，保存反馈单独保留。
- 设置页已支持修改访问密码：必须验证当前密码，新密码使用 bcrypt 哈希保存，修改后当前登录仍然有效。
- 公共布局和设置表单已加入 iPhone Safari/Chrome 所需的 viewport、安全区、窄屏换行、触摸尺寸和输入字号适配。
- 2026-09-11 山景玻璃 UI 改版已完成：根布局按路径使用指定山景视频或海报，统一深色玻璃令牌、粉色强调色、宋体标题和克制淡入动效；首页、登录、首次设置页保留视频，创建想法、详情、设置和已归档页使用海报。首页按截图要求采用左侧文案与新想法、右侧想法桌面的桌面布局；想法桌面分为上下两个可折叠的“待收口想法”和“已收口想法”分组，只有尚未归档且已生成最终提示词的 `finalized` 想法进入“已收口想法”，已归档想法从首页两组彻底排除，只在“已归档”分页显示；首页卡片只显示标题、状态、适用类型和创建时间，不显示原始正文，首页两组不提供删除按钮。顶部导航将“规则”改为“设置”，并在设置与 GitHub 图标之间增加“已归档”分页；已归档页显示全部归档想法，每条右侧提供删除按钮，顶部提供二次确认后的全部删除。标题按逗号分成两行，右上角改为 GitHub 图标入口。保留既有 API 和导出能力，同时增加 `/api/ideas/[id]/delegate`、`/api/ideas/[id]/force-finalize`、`/api/archived-ideas` 和 JSON 导出格式；首页只从活动想法字段分组，归档列表由专用接口读取。
- 山景背景支持视频加载失败回退海报和 `prefers-reduced-motion`；此前真实 HTTP/浏览器验收已覆盖首页、登录、首次设置、创建想法、想法详情和设置页。本次新增已归档页的真实 HTTP/页面验收，并检查桌面及 390×844、320×640 窄视口下无横向溢出、导航可见可点击、触摸尺寸和 16px 输入字号。
- 提示词版本的复制按钮已兼容局域网 HTTP 页面：优先使用 Clipboard API，失败时回退到临时文本框复制，并在两种方式都失败时给出长按复制提示。
- 结构化 AI 响应使用 Zod 校验；首次返回不合规时会发起一次修复请求，并保留可读的校验错误信息。
- 结构化分析在进入 Zod 前会把模型误写的对象型优先级、缺口等字段归一化为可读字符串数组，同时在提示词中明确数组元素类型；`delegatedTasks` 保持对象数组契约。
- 当前代码已通过 64 个单元测试（包含需求契约、技术分流、工作流事务、旧数据库迁移、导出、归档删除、Provider 适配、双配置选择与绑定、结构化字段归一化和 UI 行为）、TypeScript 检查、Next.js 生产构建和生产依赖审计；SQLite 初始化脚本也已在临时数据目录验证。
- Provider 适配的自动化测试覆盖普通 Bearer、Xiaomi MiMo 付费 API、Xiaomi Token Plan、通用 Coding Plan 选项和空响应/SSE 错误；这仍是本地协议验证，不等于真实服务器已用有效 Token Plan 凭证成功调用。服务器部署成功记录见 `DEPLOYMENT.md`，具体服务器日志和运行指标仍以服务器现场为准。

## 2.0 未实施的设想与边界

- 公共 GitHub 仓库、README 和 MIT LICENSE 已完成；公开源码不代表当前单用户局域网应用可以直接公网开放。
- 真实 Token Plan 凭证的最终现场调用不纳入本地自动化验证；本版本只完成协议适配和合成 Provider 验证，真实 `tp-` Key 仍需在服务器现场确认。
- 2.0 不实施自动部署、GitHub Actions、多人账号、注册/邀请、每用户数据隔离、公开公网入口或在线协作。
- 2.0 不实施 HTTPS/反向代理、安全 Cookie、登录限流、CSRF/Origin 校验、Provider SSRF 防护和面向公网的备份/隐私体系。
- 2.0 不实施语音交互、独立 Android/iOS App、Ollama、本地模型、文件上传和外部服务集成。
- 语音交互功能待跟进：优先评估“浏览器 `speechSynthesis` 朗读 + `MediaRecorder` 录音后调用外部 STT API 转文字”的方案；第一版不保存原始音频，不要求自托管 TTS 模型。当前局域网 HTTP 部署若要使用浏览器麦克风，还需先增加 HTTPS；实现前再确定语音服务商、模型/声音配置、浏览器范围和失败回退。
- 独立 Android/iOS App 待跟进：服务器由项目方集中部署，App 在用户设备运行；后续需要决定登录令牌、用户 API Key 和想法/问答内容分别存放在设备还是服务器，以及是否需要端到端加密。当前项目的单用户数据库、全局设置和 Cookie 登录不能直接作为多用户 App 后端；实现前需在 WebView 壳、原生客户端复用现有后端、或完全独立离线 App 之间做选择。
- 继续维护仓库安全边界：排除 API Key、SQLite 数据库、`.luodian-key`、`luodian_data` 和上传副本；如后续配置自动化工作流，再增加提交前密钥扫描并审查 GitHub Actions 的权限与第三方 Action 来源。
- 如果后续还要把应用本身开放给多人使用，需要另行设计用户注册/邀请、登录与密码生命周期、每用户数据隔离和所有 API 的资源归属校验；当前单用户全局数据模型不能直接复用为公网多用户版本。
- 公网版本还需要补齐 HTTPS 与 Secure Cookie、登录限流、CSRF/Origin 校验、API Key 按用户隔离加密、Provider 地址白名单与 SSRF 防护、备份与隐私边界。
- 当前阶段不配置自动部署工作流，也不做多人账号、公网入口或上述安全体系改造。

## 验收标准

- 用户能通过网页完成首次设置、登录、创建想法、回答或委派追问、正常/强制收口和复制/导出提示词。
- 用户能在首次设置和设置页分别保存普通 API 计量与 Token/Coding Plan；两组同时可用时，能在新想法首次分析前选择并沿用所选接口。
- 用户能从顶部“已归档”分页查看已归档想法，并在二次确认后删除单条或全部归档记录；首页想法分组没有删除按钮。
- 归档后的想法不出现在首页两个分组；设置页两组 Provider 的测试反馈分别显示在对应按钮右侧，保存反馈单独显示。
- 系统每轮最多提出一个关键问题，并展示问题原因。
- 技术问题自动进入 Agent 自行研究/决策；用户必要决策与可选延伸分开；未确认内容不得伪装成已确认事实写入最终提示词。
- 重启容器后想法、个人规则和提示词版本仍然存在。
- 交付目录包含完整 Compose、源码、部署说明和独立的待上传服务器副本。
- 本机已执行静态检查、单元测试、生产构建，并用隔离临时数据目录启动本地生产 HTTP 服务完成本次页面/API 验收；Debian Docker 实际运行、局域网访问和真实 AI 接口仍需在本次改版上传后复测。

## 当前验收结论

- 公共 GitHub 仓库、README、MIT LICENSE 和 `v1.0.0` 稳定恢复标签：已完成。
- 服务器上传、部署和实际测试：用户已确认成功。
- 山景玻璃 UI 改版、双 Provider 配置改造、归档首页边界和独立测试反馈及本机隔离 HTTP/浏览器验收：已完成；2.0.0 需要同步交付副本并重新构建镜像，保留现有数据卷。
- Token Plan Base URL 实际功能场景：代码已按官方 `tp-` Key、`api-key`、`max_completion_tokens` 和非流式 JSON 约定适配，但仍待使用有效凭证单独复测。
- 本 Agent 未独立 SSH 连接服务器；服务器端的详细日志、端口、防火墙和 Docker 状态不在本次记录的独立证据范围内。

## 交付边界

- 项目目录：本仓库根目录。
- 待上传副本：本地工作区中的 `待上传服务器/Project落点/` 目录。
- 服务器目标：用户实际 Docker Compose 项目目录；公共文档不写死本机路径或服务器绝对路径。
